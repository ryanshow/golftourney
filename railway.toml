[build]
builder = "nixpacks"
buildCommand = "python manage.py collectstatic --noinput && python manage.py migrate"

[deploy]
startCommand = "gunicorn golftourney.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
healthcheckPath = "/"
restartPolicyType = "on_failure"
